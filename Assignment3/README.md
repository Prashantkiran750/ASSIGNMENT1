# ASSIGNMENT3

#Task1
=> Created one variable named 'number' that takes input from user  
=> Created a function named 'factorial' with one argument which can be called to calculate the factorial of any number from user.
=> Inside the function 'factorial', a new local variable named 'num' is created and a value of '1' is passed to it initially. This num will kept on changing by syntax 'num *= number' and 'number' variable will also kept on changing by 1.
=> Finally, we return the 'num'
=> Later this function is called and printed using the print function

#Task2
=> 'Math' module is imported.
=> User input is asked and passed it to 'number'
=> Then we calculate Square root, Natural Logarithm and Sin of a number
=> Later it was printed by using print and f string.


